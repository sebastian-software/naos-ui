---
title: Overview
description: Where Naos fits next to Stencil, Lit, Solid, Gea, and React.
order: 1
---

# Naos in Context

Naos compiles typed TSX into native Custom Elements: no virtual DOM or framework
runtime, a small shared execution kernel, signals for reactivity, and an optional
batteries layer (accessible primitives, router, data resources, motion) around a
narrow Rust/OXC compiler core.

These pages compare Naos with adjacent tools. Each one is a July 2026 positioning
snapshot written to explain where Naos fits — not to rank a v0.1 prerelease
against mature, production projects. Claims about other tools are drawn from
their own docs.

## The comparisons

| Compared with | Category | The sharpest line |
| --- | --- | --- |
| [Stencil](./stencil) | Compile-to-Web-Components | Closest neighbor; Stencil uses a lightweight VDOM and generates framework wrappers, Naos has no VDOM and relies on native interop |
| [Lit](./lit) | Web Components library | Lit is a runtime library (no build step) with tagged templates; Naos is a compiler with a small shared kernel, not a template runtime |
| [Solid](./solid) | Reactive UI library | Shared signal DNA; Solid renders an app into a root, Naos makes the Custom Element the primary output |
| [Gea](./gea) | Compiler-first UI framework | Nearly identical vocabulary; Gea patches standard DOM as an app framework, Naos emits native Custom Elements |
| [React](./react) | App runtime library | A different corner: React is a VDOM app runtime; Naos is complementary and ships a small shared kernel instead of a framework runtime |

## Runtime-size snapshot

This is a **baseline-size snapshot, not an application-bundle benchmark**. It
measures a minimal browser entry for each package with tsdown/Rolldown 0.22.3,
ESM output targeted at ES2023, minification, and `gzip -9`. The values include
the dependencies pulled in by that entry, but omit application code, component
code, and any additional product packages.

| Baseline entry | Version | gzip |
| --- | --- | --- |
| Naos complete `@naos-ui/runtime/internal` contract | current workspace | 3.8 kB |
| Lit `LitElement` + `html` | 3.3.3 | 5.7 kB |
| Solid `createSignal` + DOM `render` | 1.9.14 | 3.3 kB |
| Stencil minimal `@stencil/core/internal/client` import | 4.43.5 | 2.2 kB |
| React `createElement` + React DOM `createRoot` | 19.2.7 | 59.0 kB |

The entries reflect different architectures, so the figures are useful for
orientation rather than ranking. In particular, Stencil's internal client entry
is not its entire component-runtime surface, while React's figure includes its
full DOM renderer. Gea is intentionally omitted: its runtime is assembled at an
application entry rather than exposed as one stable, comparable browser package.

## How to read them

- **Same output target** (Stencil, Lit): the honest question is *how* the element
  is produced and updated.
- **Same reactivity** (Solid, and by extension Gea): the honest question is
  *what gets shipped* and *what the default output is*.
- **Different corner** (React): the honest question is *composition* — Naos and
  React work together more naturally than they compete.

## Recurring themes

Across every comparison, the Naos-specific lines are consistent:

- **Native Custom Elements** as the primary output — Shadow DOM, slots, `part`,
  Declarative Shadow DOM prerender, and a form-associated MVP.
- **No virtual DOM or framework runtime**; generated elements share a small,
  tree-shakeable execution kernel.
- **Signals** (`state()`, `computed()`, `effect()`) instead of hooks, reactive
  properties, or proxy mutation.
- A **Rust/OXC compiler core** behind a thin TypeScript surface.
- A **narrow core plus optional batteries** boundary that CI enforces —
  foundations never import product layers.
- A candid **v0.1 prerelease** status.
