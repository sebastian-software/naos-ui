name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  quality:
    name: Quality Gates
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.6.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build local native binding
        run: pnpm build:native

      - name: Check release set
        run: pnpm check-release-set

      - name: Check native types
        run: pnpm check-native-types

      - name: Check TypeScript
        run: pnpm check-types

      - name: Check docs types
        run: pnpm check:docs

      - name: Check Rust formatting
        run: cargo fmt --check

      - name: Lint Rust
        run: pnpm lint:rust

      - name: Test packages and Rust
        run: pnpm test

      - name: Check example types
        run: pnpm --filter @iktia/example-counter type-check

      - name: Build example
        run: pnpm --filter @iktia/example-counter build

      - name: Build docs
        run: pnpm build:docs

  native:
    name: Native Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.6.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Test Rust workspace
        run: cargo test --workspace

      - name: Build local native binding
        run: pnpm build:native

  browser:
    name: Browser Gate (${{ matrix.browser }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.6.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build local native binding
        run: pnpm build:native

      - name: Prepare demo DSD
        run: pnpm --filter @iktia/example-counter prepare:dsd

      - name: Install Playwright browser
        run: pnpm --filter @iktia/example-counter exec playwright install --with-deps ${{ matrix.browser }}

      - name: Run example browser tests
        run: pnpm --filter @iktia/example-counter exec playwright test --project=${{ matrix.browser }}
